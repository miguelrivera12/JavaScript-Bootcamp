// 1. You can't define a variable more than once.

let petName = 'Milo'
petName = 'Beto'

console.log(petName)

// 2. there are rules related to variable names
let test_ = 2
let result = 3 + 4

// 3. Variable names cannot be reserverd keywords